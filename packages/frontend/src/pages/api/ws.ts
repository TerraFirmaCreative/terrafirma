import { NextApiRequest, NextApiResponse } from "next"
// import { Server } from "socket.io"
// import { io, initIo } from "@/config"

export default function handler(req: any, res: any) {
  // if (!io) {
  //   initIo(res.socket.server)
  //   res.socket.server.io = io
  // }
  res.send()
}